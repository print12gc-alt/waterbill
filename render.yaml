services:
  - type: web
    name: water-meter
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
